<div metal:use-macro="view.layout">
    <div metal:fill-slot="content">
        <div tal:condition="error">
            <div class="row alert alert-danger">${error}</div>
        </div>
        <div class="jumbotron" style="margin-top: 2em;">
            <h2>Hello!</h2>
            <p>It looks like it has been more than a day since you last
            used the GeoDigger UI. Before you continue, I need to
            take care of a few things; this should only take a
            couple minutes. You will be automatically redirected to the
            filter page once this process is complete.<br><br>
            </p>
            <p>Here's a pretty progress bar to look at while you
            wait.<br>
            </p>
            <div class="progress progress-striped active">
                <div class="progress-bar" role="progressbar"
                     aria-valuenow="${progress}" aria-valuemin="10"
                     aria-valuemax="100" style="width: ${progress}%">
                     <span class="sr-only">${progress}% Complete</span>
                </div>
            </div>
        </div>
    </div>
</div>
